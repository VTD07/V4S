var buttons=[document.getElementById('click-1'),document.getElementById('click-2'),document.getElementById('click-3')];

function updatestyles(activebutton)
{
    buttons.forEach(button =>
        {
            
        }
    )
}